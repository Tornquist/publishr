.jumbotron
  %h1
    Listing Ebooks

- @ebooks.each_slice(4) do |slice|
  .row
    - slice.each do |ebook|
      .col-sm-3
        .panel.panel-default
          .panel-body
            = ebook.title
            .btn-group.btn-group-justified
              = link_to fa_icon("desktop"), ebook, class: "btn btn-primary"
              = link_to fa_icon("pencil"), edit_ebook_path(ebook), class: "btn btn-warning"
              = link_to fa_icon("trash"), ebook, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-danger"
= link_to 'New Ebook', new_ebook_path, class: "btn btn-success"
